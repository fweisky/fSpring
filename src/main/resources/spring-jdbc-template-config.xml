<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd"
        default-lazy-init="true">

    <!-- 扫描包 -->
    <context:component-scan base-package="org.fengw.spring" />

    <!-- 导入资源文件 -->
    <context:property-placeholder location="db.properties" />

    <!-- JDBC数据源：商品 -->
    <bean id="dataSourceGoods" class="org.apache.commons.dbcp2.BasicDataSource">
        <property name="driverClassName" value="${db.goods.driverClassName}" />
        <property name="url" value="${db.goods.url}" />
        <property name="username" value="${db.goods.username}" />
        <property name="password" value="${db.goods.password}" />
    </bean>

    <!-- JdbcTemplate -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSourceGoods" />
    </bean>

    <!-- NamedParameterJdbcTemplate -->
    <bean id="namedParameterJdbcTemplate" class="org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate">
        <constructor-arg ref="dataSourceGoods" />
    </bean>

</beans>
